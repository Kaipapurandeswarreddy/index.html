{
  "name": "static-website",
  "version": "1.0.0",
  "description": "Static website with multiple pages",
  "scripts": {
    "dev": "npx http-server -p 3000 -c-1",
    "start": "npx http-server -p 3000"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
